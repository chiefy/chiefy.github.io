box: nodesource/trusty:4.3.0

build:
  steps:
    - npm-install
    - script:
        name: build site
        code: npm run build

deploy:
  gh-pages:
    - lukevivier/gh-pages@0.2.1:
        token: $GIT_TOKEN
        basedir: build
