box: nodesource/trusty:4.3.0

build:
  steps:
    - npm-install
    - script:
        name: build site
        code: npm run build

deploy:
  gh-pages:
    - pheres/git-push@0.4.10:
       gh_token: $GIT_TOKEN
       clean_removed_files: true
       basedir: build
       branch: master
