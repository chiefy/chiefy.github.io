box: nodesource/trusty:4.3.0

build:
  steps:
    - npm-install
    - script:
        name: build site
        code: npm run build

deploy:
  gh-pages:
    - leipert/git-push@0.7.6:
       gh_oauth: $GIT_TOKEN
       clean_removed_files: true
       basedir: build
